import { Text } from "@/components/atoms/Text/Text";
import styles from "./Footer.module.scss";

export const Footer = () => {
  return (
    <footer className={styles.footer}>
      <Text textColor="gray">TEST</Text>
    </footer>
  );
};
