import { FunctionComponent } from "react";

import styles from "./About.module.scss";

export const About: FunctionComponent = () => {
  return <section id="about" className={styles.about}></section>;
};
